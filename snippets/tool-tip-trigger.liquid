{% if content != blank or custom_size %}
  <tool-tip-trigger class="tool-tip-trigger" data-tool-tip="{{ context }}">
    <div class="tool-tip-trigger__title">{{ title }}</div>
    <span
      class="tool-tip-trigger__content"
      data-tool-tip-content=""
    >
      {% if custom_size %}
        {% render 'sizechart' %}
      {% else %}  
      {{ content }}
      {% endif %}
    </span>
  </tool-tip-trigger>
{% endif %}
